#!/usr/bin/env bash
set -Eeuo pipefail

cat >&2 <<'EOF'
scripts/update.sh is a project contract and has not been configured yet.
Implement the release update sequence for this application, including dependencies, build, migrations, caches, workers and health verification.
EOF
exit 1
