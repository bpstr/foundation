# Architecture decisions

Store Architecture Decision Records in this directory using `ADR-NNNN-short-title.md`.

Accepted decisions are historical. Do not silently rewrite them after the system changes. Create a new ADR, link it with `supersedes`, and mark the old ADR as superseded.

Start from `../templates/adr.md`.
