# Feature specifications

Create one focused Markdown file per meaningful feature or capability. A feature specification should explain purpose, behavior, constraints, failure cases, permissions and acceptance criteria without duplicating the implementation line by line.

Update the relevant specification in the same change that modifies behavior. Start from `../templates/feature.md`.
